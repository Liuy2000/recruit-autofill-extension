# 安全说明

本扩展设计为单机个人工具，不包含分析统计、广告、账号系统、远程模型或任何网络请求。

请不要将 `private` 目录提交到公开仓库，也不要把包含个人资料的浏览器用户目录分享给他人。若需要发布扩展，只发布 `extension` 目录或 `dist` 中不含个人资料的压缩包。

自动填写后必须人工复核。招聘网站可能使用含义相近但用途不同的字段，本扩展在匹配不确定时宁可留空，也不会自动提交。

